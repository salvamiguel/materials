import React from 'react';
import DemoWrapper from '../../shared/DemoWrapper';

export default function WorkspacesSim() {
  return (
    <DemoWrapper title="Simulador de Workspaces" description="Gestiona múltiples workspaces">
      <div style={{ padding: '2rem', textAlign: 'center', color: 'var(--ifm-color-primary)' }}>
        <p>🚧 Demo en construcción</p>
      </div>
    </DemoWrapper>
  );
}
