import React from 'react';
import DemoWrapper from '../../shared/DemoWrapper';

export default function StateDriftDetector() {
  return (
    <DemoWrapper title="Detector de Drift" description="Compara estado real vs deseado">
      <div style={{ padding: '2rem', textAlign: 'center', color: 'var(--ifm-color-primary)' }}>
        <p>🚧 Demo en construcción</p>
      </div>
    </DemoWrapper>
  );
}
