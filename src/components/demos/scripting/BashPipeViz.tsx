import React from 'react';
import DemoWrapper from '../../shared/DemoWrapper';

export default function BashPipeViz() {
  return (
    <DemoWrapper title="Visualizador de Pipes" description="Visualiza el flujo de datos en pipes">
      <div style={{ padding: '2rem', textAlign: 'center', color: 'var(--ifm-color-primary)' }}>
        <p>🚧 Demo en construcción</p>
      </div>
    </DemoWrapper>
  );
}
